# Production API origin (the Render service URL or your API subdomain).
# In local dev you can leave this blank — Vite proxies /api to localhost:4000.
VITE_API_URL=https://api.doozy.andrewfbutler.com
